@* Copyright (c) Richard D. Kiernan.
   Licensed under the Business Source License 1.1. See LICENSE for details. *@

@page "/services/patterns"
@using LinuxMadeSane.Application.Contracts.Services
@using LinuxMadeSane.Application.Interfaces
@inject IServiceOperationsService ServiceOperations

<PageTitle>Service Patterns | Linux Made Sane</PageTitle>

<div class="page-shell">
    <section class="hero-panel compact">
        <div>
            <p class="eyebrow">One-click deployment patterns</p>
            <h1>Create a service from a folder with sane defaults</h1>
            <p class="hero-copy">Start from common app types instead of writing unit files from scratch. The goal is to reduce friction and give app vendors a credible deployment story from day one.</p>
        </div>
    </section>

    @if (viewModel is null)
    {
        <section class="surface-card">
            <p class="meta-text">Loading deployment patterns...</p>
        </section>
    }
    else
    {
        <section class="feature-grid">
            @foreach (var pattern in viewModel.Patterns)
            {
                <article class="surface-card module-card active">
                    <p class="eyebrow">@pattern.Name</p>
                    <h2>@pattern.Summary</h2>
                    <p>@pattern.PlainEnglishOutcome</p>

                    <div class="decision-list">
                        @foreach (var item in pattern.SaneDefaults)
                        {
                            <InfoPill Eyebrow="Sane default" Text="@item" />
                        }
                        @foreach (var asset in pattern.GeneratedAssets)
                        {
                            <InfoPill Eyebrow="Generated asset" Text="@asset" />
                        }
                    </div>
                </article>
            }
        </section>
    }
</div>

@code {
    private ServiceDeploymentPatternsViewModel? viewModel;

    protected override async Task OnInitializedAsync()
    {
        viewModel = await ServiceOperations.GetDeploymentPatternsAsync();
    }
}
